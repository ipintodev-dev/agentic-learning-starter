version: "3.9"
services:
  math-agent:
    build: ./agents/math_agent
    environment:
      - PORT=8081
    ports: ["8081:8081"]
  cpa:
    build: ./cpa
    environment:
      - MATH_AGENT_HTTP_URL=http://math-agent:8081
      - PORT=8000
    ports: ["8000:8000"]
    depends_on:
      - math-agent