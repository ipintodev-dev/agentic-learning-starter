from typing import List, Dict

# toy spaced repetition scheduler: returns simple due items list
def due_items(user_id: str) -> List[Dict]:
    # in reality would inspect timestamps/intervals
    return []